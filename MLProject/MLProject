name: workflow-ml

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "diabetes_prediction_dataset_preprocessing.csv"}
    command: "python modeling.py"